# Local Development Variables
PORT=5000
MONGO_URI=mongodb://localhost:27017/offer_editor
JWT_SECRET=your_secret_key_here

# Render Deployment
# Set the explicit FRONTEND_URL dynamically to allow CORS on live apps
# Example:
# FRONTEND_URL=https://onboardingapp-renderdeployment.onrender.com
# For testing locally, you can use:
# FRONTEND_URL=http://localhost:5173

# Google OAuth Configuration (Usually set in setup scripts or deployment dashboard)
# GOOGLE_OAUTH_CREDENTIALS_BASE64=
# GOOGLE_OAUTH_TOKEN_BASE64=
